#include<stdio.h>
#include<math.h>
int main (){
int x=2,y=6;
int r = pow(x,y);
printf("%d\n",r);
// in the c programming we can't use % operators but use in the java language 
return 0;
}
#include<stdio.h>

int main (){



return 0;
}