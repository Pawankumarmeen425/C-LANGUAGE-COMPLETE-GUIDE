#include<stdio.h>

int main (){
//     // 1st program  conditional assignment
    int x;
    printf("Enter a number");
    scanf("%d",&x);
    // conditional operator use only one liner.
    x>0 ? printf("positive number") : printf("non positive number");

return 0;
}