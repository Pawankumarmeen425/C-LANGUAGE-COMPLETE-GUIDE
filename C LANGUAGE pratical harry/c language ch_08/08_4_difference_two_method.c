#include<stdio.h>

int main (){
    char *ptr = "pawan";
    // char ptr[] = "pawan";
    ptr = "shubham";
    printf("%s",ptr);
    //  We can reinsilize pointer string but we can't reinsilize normal array string
return 0;
}